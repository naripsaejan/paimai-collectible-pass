'use client';

// Placeholder Thirdweb provider wrapper. Replace with real ThirdwebProvider when SDK is wired.
import React from 'react';

export function ThirdwebProvider({ children }: { children: React.ReactNode }) {
	return <>{children}</>;
}


